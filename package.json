{
  "name": "thebestfan",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "build": "npm install && node build.mjs",
    "auth:smoke": "node scripts/auth-smoke.mjs",
    "foot:smoke": "node scripts/football-smoke.mjs",
    "duel:smoke": "node scripts/duel-play.mjs",
    "deck:smoke": "node scripts/deck-smoke.mjs",
    "deck:ui": "node scripts/deck-ui-smoke.mjs",
    "pages": "node scripts/verif-pages.mjs"
  },
  "dependencies": {
    "express": "^4.22.2",
    "mysql2": "^3.24.3",
    "nodemailer": "^6.10.1",
    "socket.io": "^4.8.3",
    "socket.io-client": "^4.8.3"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "jsdom": "^26.0.0",
    "typescript": "5.8"
  }
}
