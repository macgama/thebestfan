{
  "name": "thebestfan",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "build": "npm install --omit=dev",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "smoke": "tsc -p tsconfig.test.json && node scripts/duel-smoke.mjs"
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.1",
    "typescript": "^5.8.2",
    "@types/node": "^22.13.0"
  }
}
